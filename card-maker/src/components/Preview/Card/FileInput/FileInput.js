import React from 'react';
import classes from './FileInput.module.css'

const FileInput = ({ imageUploader }) => (
    <button className={classes.button}>Image</button>
  );

export default FileInput;