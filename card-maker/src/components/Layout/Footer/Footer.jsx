import React from 'react';
import classes from './Footer.module.css';

const Footer = (props) => (
    <footer className={classes.footer}>
      &copy; code your Dream
    </footer>
  );

export default Footer;